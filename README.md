Homework 28 "Дженерик-функції"

Ніякого any. Якщо здається, що без нього не обійтись - то вам здається. Пробуйте без нього.

Завдання 1 : Останній елемент
Напишіть getLast, яка приймає масив будь-якого типу і повертає останній елемент.

Copy code
const n = getLast([1, 2, 3]);    // має бути number | undefined
const s = getLast(["a", "b"]);   // має бути string | undefined
const e = getLast([]);           // undefined
Наведіть курсор на результат і переконайтесь, що тип саме такий.

Завдання 2 : Обгортка в масив
Напишіть wrapInArray, яка приймає значення і повертає масив із одного елемента.

Copy code
const a = wrapInArray(5);         // number[]
const b = wrapInArray("hello");   // string[]
Завдання 3 : Дві комірки
Напишіть swap, яка приймає пару значень різних типів і повертає їх у зворотному порядку.

Copy code
const r = swap("Аліна", 26);   // має бути [number, string]
Спробуйте спочатку написати це з одним параметром типу. Подивіться, що скаже компілятор, і запишіть текст помилки в коментар.

Завдання 4 : Полагодьте функцію
Ця функція не компілюється. Знайдіть причину і виправте.

Copy code
function filterAndTransform<T>(
arr: T[],
predicate: (item: T) => boolean,
transform: (item: T) => T
): T[] {
return arr.filter(predicate).map(transform);
}

// Після виправлення це має працювати:
const result = filterAndTransform(
[1, 2, 3, 4],
n => n % 2 === 0,
n => `Число: ${n}`
);
// result: string[]
Завдання 5 : Обмеження
Напишіть printId, яка приймає будь-який об'єкт, у якого є поле id типу число, друкує цей id і повертає об'єкт назад з повністю збереженим типом.

Copy code
const user = printId({ id: 1, email: "a@test.com" });
user.email;   // має працювати, тип не втрачено

// printId({ name: "Alice" });   // має бути помилка: немає id
// printId({ id: "abc" });       // має бути помилка: id не число
Обидва закоментовані рядки залиште у файлі разом із текстом помилок.